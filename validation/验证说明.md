# 迁移包验证

已用包内 60 个 wheel 完全离线新建 Python 3.10 环境，导出模块导入成功，8 项协议及官方 LeRobot 数据写入/加载测试全部通过。测试使用临时合成数据，测试环境已清理，程序源码和离线安装文件保留。

两端源码副本已与源文件 SHA-256 清单比对，中转机原始源码和测试文件再次比对一致。GOS 原程序及服务状态检查见 gos_source_check.json。符号链接均指向包内文件。

这是文件迁移和环境重建验证，未在全新系统安装 apt 依赖，也未重启 GOS 服务或重新执行机械臂动作验收。原程序和原数据目录保留。

校验包内文件：在包根目录运行 `sha256sum -c SHA256SUMS`。外层 tar.gz 的独立 SHA-256 清单位于压缩包同级目录。
